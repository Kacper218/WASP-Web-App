@model IEnumerable<WASP_Web_App.Entities.Permissions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Auth.Login)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Group_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Groups.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Auth.Login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Group_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Groups.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Permission_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Permission_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Permission_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
